#include <stdio.h>

typedef struct {
    int x;
    int y;
    int side;
    char* color;
} primitive;

typedef struct {
    int primitive_x;
    int primitive_y;
    char * color;
} object;

void main()
{

// сконструировать конкретные объекты структур
// интеррапт с клавиатуры меняет координаты в непрерывном режиме - одно нажатие = сдвиг
// информация об объектах преобразуется в поток данных 



}